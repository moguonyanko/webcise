/**
 * NortificationAPIを使うためだけのServiceWorkerスクリプト
 * 何も記述する必要は無いがとりあえずバージョンを定義している。
 */

const VERSION = "v1";
const APP_NAME = "webcise-nortification-sample";
const CACHE_KEY = `${APP_NAME}-${VERSION}`;

// Does nothing
